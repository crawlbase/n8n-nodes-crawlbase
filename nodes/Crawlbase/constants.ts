/** Crawlbase API base URL. Auth is via query param only. */
export const CRAWLBASE_API_BASE = 'https://api.crawlbase.com/';

/** Default request timeout in ms (Crawling API). */
export const DEFAULT_TIMEOUT_MS = 90_000;
